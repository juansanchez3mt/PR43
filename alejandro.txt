git clone // acá se escribe la url del github

cd // acá se escribe el nombre de la carpeta

git status

git add // acá se escribe el nombre del archivo o se usa --all para subir todo
git commit -m"notas"
git push

git checkout -b // acá se agrega el nombre de la rama -// el -b se usa unicamente para crear la rama

git pull